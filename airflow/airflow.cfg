"""
# notice:
airflow.cfg 的配置文件默认有800多行
这里展示的是 已经了解到的配置更改信息
"""

# dags_folder = /root/airflow/dags
dags_folder = /root/airflow/dags

# email
[smtp]
smtp_host = smtp.xxx.xxx.cn
smtp_starttls = True
smtp_ssl = False

smtp_user = help@xxx.com
smtp_password = xxx

smtp_port = xxx
smtp_mail_from = help@xxx.com

# timezone
default_timezone = Asia/Shanghai

# storage dags run info
# tables: dag   dag_run  task_instance
sql_alchemy_conn = postgresql://postgres:pwd@192.168.10.178:5432/airflow

# executor
executor = LocalExecutor

# 每个dag 同时运行数
max_active_runs_per_dag = 1
